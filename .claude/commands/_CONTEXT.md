# Quy ước chung cho mọi command

> Mỗi command dưới đây là một "playbook" cho agent. TRƯỚC khi chạy bất kỳ command nào, thực thi `hooks/pre-task.md` (đọc CLAUDE.md → AI_RULES.md → MEMORY.md → PROJECT_SUMMARY.md → Project Bible + Sprint → skills/fpt-connect/* → Taste Skill). SAU khi xong, thực thi `hooks/post-task.md`.
>
> Stack cố định: .NET 10 · Vue 3 + TS + Vite + Pinia + Tailwind + shadcn-vue · SQL Server · Google Maps API. Không đổi stack, không phá Clean Architecture, không placeholder/TODO, không ghi đè Taste Skill (chỉ kế thừa).
